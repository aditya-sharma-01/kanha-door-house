rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Allow public read and write for installation photos and specs
    match /{allPaths=**} {
      allow read, write: if true;
    }

  }
}
